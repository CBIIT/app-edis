{
  "name": "lambda-load-from-vds",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-prod": "rm -rf node_modules && npm install --only='prod'",
    "zip-prod": "mkdir -p out && rm -rf out/lambda-load-from-vds.zip && zip -r -q out/lambda-load-from-vds.zip node_modules src/*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ldapjs": "^2.3.2",
    "parquetjs": "^0.11.2",
    "console-stamp": "^3.0.6"
  },
  "devDependencies": {
    "aws-sdk": "latest",
    "aws-xray-sdk-core": "^3.3.3"
  }
}
